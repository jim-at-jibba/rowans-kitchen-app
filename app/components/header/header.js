import React, { Component } from "react";
import { Header, Title, Body } from "native-base";
import styles from "./styles";

export default () =>
  <Header style={{ backgroundColor: "#7ee1e6" }}>
    <Body>
      <Title style={{ color: "#51565f" }}>Rowans Kitchen</Title>
    </Body>
  </Header>;
